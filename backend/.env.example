# ── Supabase ──────────────────────────────────────────────────
# Project URL (Settings → API → Project URL)
SUPABASE_URL=https://zpikaoimoqpibqdnnlhk.supabase.co

# Publishable / anon key (Settings → API → Project API keys → anon public)
SUPABASE_ANON_KEY=sb_publishable_NxIILKUJ9IqoRwA8BuA13Q_J0E8WV_-

# Service role key — KEEP SECRET, never commit this (Settings → API → service_role)
SUPABASE_SERVICE_KEY=<paste-service-role-key-here>

# JWT secret (Settings → API → JWT Settings → JWT Secret)
SUPABASE_JWT_SECRET=<paste-jwt-secret-here>

# ── App ───────────────────────────────────────────────────────
# Frontend origin for CORS (GitHub Pages URL once deployed)
FRONTEND_ORIGIN=https://vish-karthikeyan.github.io

# Super admin email
SUPER_ADMIN_EMAIL=viswajeethgk@gmail.com
